<% @amount_of_contacts = 0 %>
<% @contacts.each do |contact| %>
<% @amount_of_contacts += 1 %>
<% end %>


    <div class="pinkbar">
      <h4>There are</h4><h4 class="emphasis"><%= p @amount_of_contacts %></h4><h4>contacts</h4>

    </div>
    <div class="contact-list">

      <% @contacts.each do |contact| %>

      <ul>

        <li>
          <a href="/contacts/<%= contact.id %>"><%= contact.full_name %></a> (<%= contact.email %>)<br>
-        <p><a href="#">Edit</a> | <a href="#">Delete</a></p>
+        <p><a href="contacts/<%= contact.id %>/edit">Edit</a> | <a href="contacts/<%= contact.id %>">Delete</a></p>
        </li>

      </ul>
      <% end %>
    </div>
